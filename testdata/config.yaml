receivers:
  examplereceiver:

processors:
  exampleprocessor:

exporters:
  oragent:
  oragent/2:
    endpoint: "8.8.8.8:1234"
    api_key: "foo"

service:
  pipelines:
    traces:
      receivers: [examplereceiver]
      processors: [exampleprocessor]
      exporters: [oragent]
